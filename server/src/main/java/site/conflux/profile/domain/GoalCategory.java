package site.conflux.profile.domain;

/** Structured goals are matching signal, not an unbounded profile paragraph. */
public enum GoalCategory {
    BUILD,
    LEARN,
    CONNECT,
    CAREER,
    COLLABORATE
}
