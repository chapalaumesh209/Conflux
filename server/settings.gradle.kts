plugins {
    // Keeps the Java 21 product contract testable on contributors' machines.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "conflux-server"
