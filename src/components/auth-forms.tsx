"use client";

import { FormEvent, useEffect, useState } from "react";
import { ArrowRight, Check, Info, Mail, ShieldCheck } from "lucide-react";
import { useRouter } from "next/navigation";
import { getSupabaseClient } from "../lib/supabase/client";

const interests = ["Learn", "Build", "Connect", "Collaborate", "Find feedback", "Explore ideas"];
const skills = ["React", "Next.js", "JavaScript", "TypeScript", "Python", "Java", "AI / ML", "Product design", "DevOps", "Mobile"];
const connectionGoals = ["A co-builder", "A mentor", "A collaborator", "Feedback partners", "People in my field", "Interesting builders"];

function Message({ error, notice }: { error: string; notice: string }) { return <>{error && <p className="auth-message error">{error}</p>}{notice && <p className="auth-message success"><Check size={15} />{notice}</p>}</>; }

function PasswordInput({ value, onChange, label = "Password" }: { value: string; onChange: (value: string) => void; label?: string }) {
  const [visible, setVisible] = useState(false);
  return <label className="cf-field"><span>{label}</span><div className="password-box"><input type={visible ? "text" : "password"} value={value} onChange={(event) => onChange(event.target.value)} minLength={8} required autoComplete={label === "Password" ? "current-password" : "new-password"} /><button type="button" onClick={() => setVisible(!visible)} aria-label={visible ? "Hide password" : "Show password"} title={visible ? "Hide password" : "Show password"}><Info size={17} /></button></div></label>;
}

export function SignInForm() {
  const router = useRouter(); const supabase = getSupabaseClient();
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [working, setWorking] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); setWorking(true); setError(""); const { data, error: signInError } = await supabase.auth.signInWithPassword({ email, password }); setWorking(false); if (signInError) { setError(signInError.message); return; } const { data: profile } = await supabase.from("cf_profiles").select("onboarding_complete").eq("id", data.user.id).maybeSingle(); router.push(profile?.onboarding_complete ? "/meet" : "/onboarding"); };
  return <AuthFrame kicker="Welcome back" title={<>Continue the<br />conversation.</>} detail="Sign in to return to your private connections and Build Rooms."><form onSubmit={submit}><Message error={error} notice="" /><label className="cf-field"><span>Email</span><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required autoComplete="email" /></label><PasswordInput value={password} onChange={setPassword} /><button className="cf-primary" disabled={working}>{working ? "Signing in…" : "Sign in"}<ArrowRight size={17} /></button></form><button className="cf-text-button" onClick={() => router.push("/register")}>New to CONFLUX? Create an account</button></AuthFrame>;
}

export function RegisterFlow() {
  const router = useRouter(); const supabase = getSupabaseClient();
  const [step, setStep] = useState<"account" | "otp">("account"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [mobile, setMobile] = useState(""); const [token, setToken] = useState(""); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [working, setWorking] = useState(false);
  const create = async (event: FormEvent) => { event.preventDefault(); setWorking(true); setError(""); const { error: signUpError } = await supabase.auth.signUp({ email, password, options: { data: { mobile_number: mobile.trim() || null } } }); setWorking(false); if (signUpError) { setError(signUpError.message); return; } setStep("otp"); setNotice(`We sent a verification code to ${email}.`); };
  const verify = async (event: FormEvent) => { event.preventDefault(); setWorking(true); setError(""); const { error: verifyError } = await supabase.auth.verifyOtp({ email, token, type: "signup" }); setWorking(false); if (verifyError) { setError(verifyError.message); return; } router.push(`/onboarding?mobile=${encodeURIComponent(mobile.trim())}`); };
  const resend = async () => { setWorking(true); setError(""); const { error: resendError } = await supabase.auth.resend({ type: "signup", email }); setWorking(false); if (resendError) setError(resendError.message); else setNotice("A new verification code is on its way."); };
  if (step === "otp") return <AuthFrame kicker="Step 1 of 2" title={<>Verify your<br />email.</>} detail="Enter the one-time code sent to your inbox before adding your builder identity."><form onSubmit={verify}><Message error={error} notice={notice} /><label className="cf-field"><span>Email</span><input value={email} readOnly /></label><label className="cf-field"><span>One-time code</span><input className="otp-input" inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" maxLength={6} value={token} onChange={(event) => setToken(event.target.value.replace(/\D/g, ""))} required /></label><button className="cf-primary" disabled={working || token.length !== 6}>{working ? "Verifying…" : "Verify email"}<ArrowRight size={17} /></button></form><button className="cf-text-button" disabled={working} onClick={() => void resend()}>Resend code</button></AuthFrame>;
  return <AuthFrame kicker="Start your identity" title={<>A real account<br />starts here.</>} detail="Email verification comes first. Your mobile number is optional and is never public."><form onSubmit={create}><Message error={error} notice={notice} /><label className="cf-field"><span>Email</span><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required autoComplete="email" /></label><PasswordInput value={password} onChange={setPassword} label="Create password" /><label className="cf-field"><span>Mobile number <em>Optional</em></span><input type="tel" value={mobile} onChange={(event) => setMobile(event.target.value)} autoComplete="tel" /></label><button className="cf-primary" disabled={working}>{working ? "Sending code…" : "Continue with email"}<Mail size={17} /></button></form><button className="cf-text-button" onClick={() => router.push("/auth")}>Already have an account? Sign in</button></AuthFrame>;
}

export function ProfileOnboarding() {
  const router = useRouter(); const supabase = getSupabaseClient();
  const [checking, setChecking] = useState(true); const [error, setError] = useState(""); const [working, setWorking] = useState(false); const [selectedInterests, setSelectedInterests] = useState<string[]>([]); const [selectedSkills, setSelectedSkills] = useState<string[]>([]); const [selectedGoals, setSelectedGoals] = useState<string[]>([]);
  const [form, setForm] = useState({ name: "", username: "", headline: "", build: "", birthday: "", gender: "", linkedin: "", github: "", portfolio: "", mobile: "" });
  useEffect(() => { void supabase.auth.getSession().then(({ data: { session } }) => { if (!session) router.replace("/auth"); else setChecking(false); }); }, [router, supabase]);
  const choose = (value: string, values: string[], setValues: (items: string[]) => void) => setValues(values.includes(value) ? values.filter((item) => item !== value) : [...values, value]);
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!selectedInterests.length || !selectedSkills.length || !selectedGoals.length) { setError("Choose at least one interest, skill, and connection goal."); return; } setWorking(true); setError(""); const { error: saveError } = await supabase.rpc("cf_complete_onboarding", { requested_username: form.username, requested_full_name: form.name, requested_headline: form.headline, requested_skills: selectedSkills, requested_interests: selectedInterests, requested_build: form.build, requested_connections: selectedGoals.join(", "), requested_date_of_birth: form.birthday, requested_gender: form.gender, requested_linkedin_url: form.linkedin, requested_github_url: form.github, requested_portfolio_url: form.portfolio || null, requested_mobile_number: form.mobile || null }); setWorking(false); if (saveError) { setError(saveError.message); return; } router.push("/meet"); };
  if (checking) return <main className="cf-loading"><span className="cf-pulse" />Checking your verified session</main>;
  return <main className="cf-onboarding"><form onSubmit={submit} className="identity-form"><button className="cf-brand" type="button" onClick={() => router.push("/")}>CONFLUX<span>.</span></button><p className="cf-kicker">Step 2 of 2 · your builder identity</p><h1>Make the first introduction count.</h1><p>Every required answer gives a future conversation more useful context. LinkedIn and GitHub are required; your portfolio is optional.</p><Message error={error} notice="" /><section className="identity-section"><h2>About you</h2><div className="cf-form-grid"><label className="cf-field"><span>Your name</span><input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required /></label><label className="cf-field"><span>Username</span><input value={form.username} onChange={(event) => setForm({ ...form, username: event.target.value.toLowerCase().replace(/[^a-z0-9_]/g, "") })} required minLength={3} maxLength={30} /></label><label className="cf-field"><span>Headline</span><input value={form.headline} onChange={(event) => setForm({ ...form, headline: event.target.value })} required maxLength={180} placeholder="What you do, in a few words" /></label><label className="cf-field"><span>Date of birth</span><input type="date" value={form.birthday} onChange={(event) => setForm({ ...form, birthday: event.target.value })} required /></label><label className="cf-field"><span>Gender</span><select value={form.gender} onChange={(event) => setForm({ ...form, gender: event.target.value })} required><option value="">Select one</option><option value="WOMAN">Woman</option><option value="MAN">Man</option><option value="NON_BINARY">Non-binary</option><option value="SELF_DESCRIBE">Self-describe</option><option value="PREFER_NOT_TO_SAY">Prefer not to say</option></select></label><label className="cf-field"><span>Mobile number <em>Optional</em></span><input type="tel" value={form.mobile} onChange={(event) => setForm({ ...form, mobile: event.target.value })} /></label></div></section><ChoiceGroup title="What interests you?" options={interests} selected={selectedInterests} onChoose={(value) => choose(value, selectedInterests, setSelectedInterests)} /><ChoiceGroup title="What are your skills?" options={skills} selected={selectedSkills} onChoose={(value) => choose(value, selectedSkills, setSelectedSkills)} /><ChoiceGroup title="Who do you want to connect with?" options={connectionGoals} selected={selectedGoals} onChoose={(value) => choose(value, selectedGoals, setSelectedGoals)} /><section className="identity-section"><h2>What do you want to build?</h2><label className="cf-field"><span>Your current project or idea</span><textarea value={form.build} onChange={(event) => setForm({ ...form, build: event.target.value })} required maxLength={240} placeholder="Describe the thing you want to move forward." /></label></section><section className="identity-section"><h2>Required proof</h2><div className="cf-form-grid"><label className="cf-field"><span>LinkedIn URL</span><input type="url" value={form.linkedin} onChange={(event) => setForm({ ...form, linkedin: event.target.value })} required placeholder="https://linkedin.com/in/…" /></label><label className="cf-field"><span>GitHub URL</span><input type="url" value={form.github} onChange={(event) => setForm({ ...form, github: event.target.value })} required placeholder="https://github.com/…" /></label><label className="cf-field"><span>Portfolio URL <em>Optional</em></span><input type="url" value={form.portfolio} onChange={(event) => setForm({ ...form, portfolio: event.target.value })} placeholder="https://…" /></label></div></section><button className="cf-primary" disabled={working}>{working ? "Creating your profile…" : "Complete profile"}<ArrowRight size={17} /></button></form></main>;
}

function ChoiceGroup({ title, options, selected, onChoose }: { title: string; options: string[]; selected: string[]; onChoose: (value: string) => void }) { return <section className="identity-section"><h2>{title} <small>Required</small></h2><div className="choice-grid">{options.map((option) => <button type="button" className={selected.includes(option) ? "selected" : ""} key={option} onClick={() => onChoose(option)}>{selected.includes(option) && <Check size={14} />}{option}</button>)}</div></section>; }

function AuthFrame({ kicker, title, detail, children }: { kicker: string; title: React.ReactNode; detail: string; children: React.ReactNode }) { return <main className="cf-auth"><section><button className="cf-brand" onClick={() => window.location.assign("/")}>CONFLUX<span>.</span></button><p className="cf-kicker">{kicker}</p><h1>{title}</h1><p>{detail}</p>{children}</section><aside><span><ShieldCheck size={27} /> Your identity stays yours. Contact links unlock only after a mutual connection.</span></aside></main>; }
